# core — required
requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.13

# optional — only needed for --extract (text out of downloaded files)
# pymupdf4llm converts PDFs to real markdown (headings, pipe tables); pypdf is
# the fallback when it is absent, and gives flat text instead.
pymupdf4llm>=0.0.17
pypdf>=4.0
python-pptx>=0.6.23
python-docx>=1.1
openpyxl>=3.1

# optional — only needed for --ocr (scanned PDFs, macOS only)
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0
